# Arquitectura de Software - Modelo C4

Proyecto: PathSeek
Código del documento: DOC-012
Versión: V_1_1_0
Fecha: 2026-09-01
Responsable: Equipo del proyecto

---

## 1. Nivel 1: Diagrama de Contexto (C4 L1)

### 1.1 Diagrama

```mermaid
C4Context
    title PathSeek - Diagrama de Contexto (L1) - UGEL Huancayo

    Person(admin, "Administrador", "Gestiona configuración, usuarios, reportes globales. CRUD total. (USR-01)")
    Person(operador, "Operador Logístico", "Registra pedidos/flota/conductores, genera rutas, supervisa dashboard. (USR-02)")
    Person(conductor, "Conductor", "Consulta ruta asignada, entregas, alertas tráfico. Modo conductor. (USR-03)")
    Person(cliente, "Cliente", "Registra preferencias entrega, consulta estado pedido propio. (USR-04)")
    Person(auditor, "Auditor Externo", "Solo lectura: logs y reportes cumplimiento. (USR-05)")

    System(pathseek, "Sistema PathSeek", "Optimizador de Rutas Sostenibles VRPTW/Green VRP para UGEL Huancayo. Angular + Node/Express + PostgreSQL + Redis")

    System_Ext(oauth, "Servicio OAuth2 / IdP", "Autenticación externa, emisión JWT, refresh tokens")
    System_Ext(traffic, "APIs Tráfico Waze / Google", "Datos congestión tiempo real y fallback carga manual (ASU-07/08)")
    System_Ext(osm, "OpenStreetMap + Leaflet", "Teselas mapas, geocodificación, PostGIS (RES-07, código abierto)")
    System_Ext(email, "Servicio Email / PDF", "Envío notificaciones y generación reportes descargables RF-006")

    Rel(admin, pathseek, "Usa", "HTTPS/TLS 1.3, REST/JSON")
    Rel(operador, pathseek, "Usa", "HTTPS/TLS 1.3, REST/JSON")
    Rel(conductor, pathseek, "Usa (offline cache)", "HTTPS/TLS 1.3, REST/JSON, ≥50kbps RNF-008")
    Rel(cliente, pathseek, "Usa", "HTTPS/TLS 1.3, REST/JSON")
    Rel(auditor, pathseek, "Consulta", "HTTPS/TLS 1.3, REST/JSON, solo lectura")

    Rel(pathseek, oauth, "Autentica/Autoriza", "OAuth2/OIDC, JWT, RBAC RN-001")
    Rel(pathseek, traffic, "Consulta tráfico", "HTTPS/JSON, polling + eventos")
    Rel(pathseek, osm, "Visualiza rutas", "HTTPS/Leaflet, teselas OSM")
    Rel(pathseek, email, "Genera reportes/notifica", "HTTPS, PDF ISO 14083")

    UpdateLayoutConfig($c4ShapeInRow="5", $c4BoundaryInRow="1")
```

> **Fallback `graph TD` si el renderer no soporta `C4Context`:** ver Apéndice A.

### 1.2 Actores vs RF

| Actor | RF principales | Descripción |
| ----- | -------------- | ----------- |
| Administrador (USR-01) | RF-001, RF-002, RF-008, RF-010 | CRUD total flota/pedidos/conductores/compensación, RNF-003/009 |
| Operador Logístico (USR-02) | RF-001..007 | Gestión operativa diaria, generación rutas RF-003, dashboard RF-005 |
| Conductor (USR-03) | RF-004, RF-007, RF-008 | Modo conductor RN-010, RNF-007/008 offline |
| Cliente (USR-04) | RF-002, RF-009 | Registro preferencias, READ pedido propio |
| Auditor (USR-05) | RF-006 | READ logs/reportes, Ley 29733 |

### 1.3 Sistemas externos y RNF asociados

| Sistema Externo | Protocolo | RNF | Mitigación |
| --------------- | --------- | --- | ---------- |
| OAuth2 / JWT | OAuth2/OIDC, JWT short-lived + refresh | RNF-003 Seguridad OWASP, Ley 29733 | AES-256 reposo, TLS 1.3 tránsito, bcrypt |
| Waze/Google Traffic | HTTPS/JSON | RNF-001/002 Rendimiento 45s/30s, RNF-005 1000 pedidos | Carga manual fallback, ponderación fuentes (ASU-08) |
| OSM/Leaflet/PostGIS | HTTPS/Leaflet, PostGIS geo | RNF-006 WCAG AA, RES-07 código abierto | Teselas cacheadas, responsive |
| Email/PDF | HTTPS | RNF-009 Documentación, ISO 14083 | PDF con huella CO₂ |

---

## 2. Nivel 2: Diagrama de Contenedores (C4 L2)

### 2.1 Diagrama

```mermaid
C4Container
    title PathSeek - Diagrama de Contenedores (L2)

    Person(admin, "Administrador", "USR-01")
    Person(operador, "Operador", "USR-02")
    Person(conductor, "Conductor", "USR-03 - offline cache")

    System_Boundary(pathseek, "Sistema PathSeek") {
        Container(spa, "SPA Angular 17+", "TypeScript, Angular, Leaflet, PWA", "UI: mapa interactivo RF-004, dashboard RF-005, formularios RF-001/002/008/009, reportes RF-006/010. WCAG 2.1 AA RNF-006, responsive RNF-008")
        Container(api, "API Backend Node.js/Express", "Node 20, Express, TypeScript, JWT, RBAC", "REST/JSON. Lógica negocio RN-001..012. Orquesta RF-003/007. RNF-003/004/005")
        Container(optimizador, "Módulo Optimización", "TypeScript, GA/Tabú/ACO/SA", "Resuelve VRPTW/Green VRP. SLA RNF-001 ≤45s/150p/15v P95, RNF-002 ≤30s re-opt. RN-003..008")
        ContainerDb(postgres, "PostgreSQL + PostGIS", "PostgreSQL 16, PostGIS, 3FN", "Persistencia relacional 8 entidades doc11. RPO ≤5s RNF-004, índice geo")
        ContainerDb(redis, "Redis Cache", "Redis 7", "Sesiones JWT, caché consultas, rate-limit RN-001 (3 intentos/15min), cola re-opt")
    }

    System_Ext(oauth, "OAuth2 IdP", "JWT/OIDC")
    System_Ext(traffic, "Waze/Google", "Tráfico")
    System_Ext(osm, "OSM Tiles", "Mapas")

    Rel(admin, spa, "Usa", "HTTPS/TLS 1.3")
    Rel(operador, spa, "Usa", "HTTPS/TLS 1.3")
    Rel(conductor, spa, "Usa offline-first", "HTTPS, ServiceWorker cache ≥50kbps")

    Rel(spa, api, "REST/JSON", "HTTPS, JWT Bearer, RBAC")
    Rel(api, optimizador, "Invoca", "In-process / Worker Thread, JSON Solution")
    Rel(api, postgres, "Lee/Escribe", "TCP 5432, SQL, PostGIS")
    Rel(api, redis, "Cache/Sesión/Cola", "TCP 6379, AES-256")
    Rel(api, oauth, "Valida token", "HTTPS/OIDC")
    Rel(api, traffic, "Consulta congestión", "HTTPS/JSON")
    Rel(spa, osm, "Teselas mapa", "HTTPS/Leaflet")

    UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

### 2.2 Catálogo de contenedores y trazabilidad

| Contenedor | Tecnología | RF cubiertos | RNF/RES clave | Escalabilidad |
| ---------- | ---------- | ------------ | ------------- | ------------- |
| SPA Angular | Angular 17, TS, Leaflet, PWA ServiceWorker | RF-004 Mapa, RF-005 Dashboard, RF-006 Reportes, RF-010 Compensación, RF-001/002/008 Forms | RNF-006 WCAG AA, RNF-007 usabilidad conductores, RNF-008 ≥50kbps offline, RNF-009 Swagger | CDN cache, lazy modules |
| API Backend | Node 20 + Express + TS | RF-001 Flota, RF-002 Pedidos, RF-003 Rutas, RF-007 Re-opt, RF-008 Conductores, RF-009 Clientes | RNF-003 OWASP 0% críticas, RNF-004 99.5% RTO30s, RNF-005 1000pd/50v | Async I/O, horizontal + LB |
| Módulo Optimización | TS GA/SA/ACO/Tabú aislado | RF-003, RF-007 | RNF-001 45s P95, RNF-002 30s, RN-003 placa, RN-004 jornada, RN-005 capacidad, RN-006 ventana, RN-007 prioridad, RN-008 CO₂ | Worker threads, warm start re-opt |
| PostgreSQL+PostGIS | PG 16 + PostGIS + 3FN | RF-001/002/008/009 (CRUD), RF-003 rutas | RNF-004 RPO5s, RES-07 open source, ISO 25010 | Réplica lectura, índices `idx_pedidos_estado`, `idx_rutas_fecha`, `GIST(gps)` |
| Redis | Redis 7 | Transversal: sesiones RN-001 | RNF-001/002 caché, RNF-004 disponibilidad | Cluster, persistencia AOF |
| Externos OSM/Waze/OAuth | OSM/Leaflet, Waze/Google, OAuth2 | RF-004 tráfico colores, Auth | RES-07 open source, Ley 29733 | Circuit breaker + fallback manual |

### 2.3 Flujos críticos

* **Generación rutas (RF-003):** `SPA -> API -> (Postgres pedidos+vehículos+conductores) -> Redis lock -> Optimizador (45s) -> Postgres rutas -> Redis pub -> SPA mapa+dashboard`
* **Re-optimización (RF-007):** `Evento (nuevo pedido/cancelación/accidente) -> Redis cola -> API -> Optimizador warm-start (30s) -> Notifica conductores (WebSocket/polling)`
* **Auth (RN-001/RNF-003):** `SPA -> API -> OAuth2/JWT -> Redis (intentos fallidos 3/15min) -> Postgres usuarios`

---

## 3. Nivel 3: Diagrama de Componentes (C4 L3) - API Backend

### 3.1 Diagrama

```mermaid
C4Component
    title PathSeek - Componentes API Backend Node/Express (L3)

    Container_Boundary(api, "API Backend - Node.js/Express") {
        Component(router, "Router / Controllers", "Express Router, zod/joi", "Expone REST RF-001..010, valida DTO, RBAC por rol doc08. RNF-003")
        Component(auth, "Auth & RBAC Middleware", "JWT, bcrypt, OAuth2", "Autentica, autoriza RBAC, rate-limit RN-001 (3/15min), audita. Ley 29733")
        Component(validation, "Validation & Sanitization", "zod, DOMPurify", "Valida RN-005 capacidad, RN-006 ventana, RN-012 duplicado, OWASP XSS/SQLi")
        Component(services, "Services de Negocio", "TypeScript", "Orquesta RN-002 IGV 18%, RN-003 placa DS033, RN-004 jornada 8h, RN-007 prioridad, RN-008 CO2, RN-009 TCO, RN-011 árboles*50")
        Component(repos, "Repositories / DAO", "pg, PostGIS", "Acceso 8 tablas doc11, 3FN, índices, consultas geo. RNF-005")
        Component(optimizerAdapter, "Optimizer Adapter", "Worker Threads", "Facade hacia Módulo Optimización. Timeout RNF-001/002, circuit breaker")
        Component(eventBus, "Event Bus / Queue", "Redis Pub/Sub + BullMQ", "Cola re-optimización RF-007, notificaciones conductores, debounce cambios múltiples")
        Component(reportGen, "Report Generator", "PDFKit", "Genera PDF RF-006/010: CO₂ ISO14083, TCO, créditos carbono, árboles")
        Component(cacheSvc, "Cache Service", "Redis Client", "Sesiones, caché pedidos/vehículos, invalidación")
    }

    ContainerDb(postgres, "PostgreSQL+PostGIS", "SQL", "11.DDL")
    ContainerDb(redis, "Redis", "Cache/Queue", "Sesiones")
    Container(optimizador, "Módulo Optimización", "TS", "VRPTW")
    System_Ext(traffic, "Waze/Google", "Tráfico")
    System_Ext(osm, "OSM", "Mapas")

    Rel(router, auth, "Delega", "JWT Bearer")
    Rel(router, validation, "Valida", "DTO sanitizado")
    Rel(validation, services, "Invoca", "datos válidos")
    Rel(auth, services, "Autoriza", "RBAC doc08")
    Rel(services, repos, "Persiste/Consulta", "SQL")
    Rel(services, optimizerAdapter, "Optimiza", "VRPTW params")
    Rel(services, eventBus, "Publica evento", "re-opt RF-007")
    Rel(services, reportGen, "Genera", "PDF RF-006")
    Rel(services, cacheSvc, "Cachea", "Redis")
    Rel(repos, postgres, "SQL", "TCP 5432")
    Rel(cacheSvc, redis, "GET/SET", "TCP 6379")
    Rel(optimizerAdapter, optimizador, "resolver_vrp()", "Worker")
    Rel(eventBus, redis, "Pub/Sub", "BullMQ")
    Rel(services, traffic, "Consulta", "HTTPS")

    UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

### 3.2 Responsabilidades y reglas

| Componente | Responsabilidad | RN/RF |
| ---------- | --------------- | ----- |
| Router/Controllers | REST endpoints, versionado `/api/v1`, OpenAPI/Swagger RNF-009 | RF-001..010 |
| Auth & RBAC | JWT short+refresh, bcrypt 12 rounds, RBAC matriz doc08, bloqueo RN-001 | RN-001, RNF-003 |
| Validation | zod schemas, sanitización OWASP, valida ventana `fin>inicio`, duplicado RN-012 | RN-005/006/012, RNF-003 |
| Services | Reglas negocio, orquesta transacciones, calcula IGV 18% RN-002, TCO RN-009, CO₂ RN-008 | RN-002..011 |
| Repositories | DAO 8 entidades, PostGIS `ST_Distance`, paginación RNF-005 | Doc11 DDL |
| Optimizer Adapter | Timeout 45s/30s, retry 1, fallback heurística greedy | RNF-001/002 |
| Event Bus | BullMQ cola re-opt, debounce 2s para cambios múltiples RF-007 | RF-007, RNF-002 |
| Report Generator | PDF ISO14083, créditos carbono, árboles = ton*50 RN-011 | RF-006/010, RN-011 |
| Cache Service | Cache-aside, TTL 5min pedidos, sesión 30min inactividad doc08 | RNF-001/004 |

---

## 4. Nivel 4: Diagrama de Código (L4) - Dominio Representativo

### 4.1 Diagrama de clases de dominio

```mermaid
classDiagram
    class Usuario {
        +uuid usuario_id
        +string email
        +string password_hash
        +string rol
        +string estado
        +autenticar(password) boolean
        +estaBloqueado() boolean
    }
    class Conductor {
        +uuid conductor_id
        +uuid usuario_id
        +string dni
        +string licencia
        +int experiencia
        +boolean disponible
        +puedeConducir(fecha, horasAcum) boolean
        +requiereDescanso() boolean
    }
    class Vehiculo {
        +uuid vehiculo_id
        +string placa
        +string tipo
        +decimal capacidad_kg
        +decimal capacidad_m3
        +decimal consumo_km_l
        +decimal factor_emision
        +int restriccion_placa_digito
        +esRestringidoPorPlaca(dia) boolean
        +calcularCO2(distancia) decimal
        +capacidadSuficiente(peso, volumen) boolean
    }
    class Cliente {
        +uuid cliente_id
        +uuid usuario_id
        +string punto_referencia
        +string horario_atencion
    }
    class Pedido {
        +uuid pedido_id
        +uuid cliente_id
        +decimal gps_lat
        +decimal gps_lon
        +decimal peso
        +decimal volumen
        +time ventana_inicio
        +time ventana_fin
        +string prioridad
        +string tipo_producto
        +string estado
        +esVentanaValida() boolean
        +esDuplicado(otro) boolean
    }
    class Ruta {
        +uuid ruta_id
        +date fecha
        +uuid conductor_id
        +uuid vehiculo_id
        +decimal distancia_km
        +decimal co2_kg
        +decimal combustible_l
        +string estado
        +calcularTCO() decimal
        +calcularArbolesCompensacion() int
    }
    class RutaPedido {
        +uuid ruta_id
        +uuid pedido_id
        +int orden
        +time hora_estimada
        +boolean cumplio_ventana
    }
    class Auditoria {
        +uuid auditoria_id
        +uuid usuario_id
        +string accion
        +string entidad
        +timestamp fecha
    }
    class Optimizador {
        +resolverVRP(pedidos, vehiculos, conductores) Solution
        +reoptimizar(evento) Solution
        +evaluarFitness(solution) decimal
    }
    class RutaService {
        +generarRutas(filtro) Ruta[]
        +reoptimizar(evento) Ruta[]
        +validarRestricciones() void
    }

    Usuario "1" --> "0..1" Conductor : usuario_id
    Usuario "1" --> "0..1" Cliente : usuario_id
    Usuario "1" --> "*" Auditoria : genera
    Conductor "1" --> "*" Ruta : conduce
    Vehiculo "1" --> "*" Ruta : asigna
    Cliente "1" --> "*" Pedido : solicita
    Ruta "1" --> "*" RutaPedido : contiene
    Pedido "1" --> "*" RutaPedido : se_incluye
    RutaService --> Optimizador : usa
    RutaService --> Vehiculo : valida
    RutaService --> Pedido : valida
    Ruta --> RutaPedido
    Pedido --> RutaPedido
```

### 4.2 Secuencia crítica: Generación de rutas

```mermaid
sequenceDiagram
    participant SPA as "SPA Angular"
    participant API as "API Controller"
    participant SVC as "RutaService"
    participant REPO as "Repositorios"
    participant OPTIM as "Optimizador"
    participant DB as "PostgreSQL"
    participant CACHE as "Redis"
    participant EVT as "EventBus"

    SPA->>API: POST /api/v1/rutas/generar
    API->>API: Auth JWT y RBAC Operador Admin
    API->>SVC: generarRutas
    SVC->>CACHE: GET lock generacion
    SVC->>REPO: obtener pendientes y vehiculos y conductores
    REPO->>DB: SELECT pedidos PENDIENTE con PostGIS
    DB-->>REPO: 150 pedidos 15 vehiculos
    SVC->>SVC: validar restricciones
    SVC->>OPTIM: resolverVRP timeout 45s
    OPTIM-->>SVC: Solution rutas co2 distancia
    SVC->>DB: INSERT rutas y ruta_pedidos
    SVC->>CACHE: SET rutas fecha y PUB evento
    SVC->>EVT: publish rutas generadas
    SVC-->>API: 200 rutas metricas
    API-->>SPA: JSON notificacion
    SPA->>SPA: Render Leaflet y Dashboard
```

---

## 5. Nivel de Despliegue (C4 L0 - Complementario)

```mermaid
graph TD
    subgraph Client["Cliente - Navegador / Movil"]
        Browser["Browser Angular PWA<br/>ServiceWorker offline"]
    end
    subgraph CDN["CDN / Static"]
        CDNNode["CDN Angular build<br/>WCAG AA"]
    end
    subgraph Cloud["Cloud - VPC"]
        LB["Load Balancer<br/>TLS 1.3, WAF OWASP"]
        API1["API Node/Express #1<br/>BullMQ worker"]
        API2["API Node/Express #2<br/>BullMQ worker"]
        OPTW["Optimizer Worker Pool<br/>Worker Threads"]
        RedisCl["Redis Cluster<br/>AOF"]
        PGPrimary["PostgreSQL Primary<br/>PostGIS, 3FN"]
        PGReplica["PostgreSQL Replica<br/>Read-only, GIST gps"]
        Storage["S3 / Storage<br/>PDF reportes"]
    end
    subgraph Ext["Externos"]
        OAuth["OAuth2 IdP"]
        TrafficAPI["Waze/Google"]
        OSMTiles["OSM Tiles"]
    end

    Browser -->|"HTTPS 443"| LB
    CDNNode --> Browser
    LB --> API1
    LB --> API2
    API1 --> RedisCl
    API2 --> RedisCl
    API1 --> PGPrimary
    API2 --> PGPrimary
    PGPrimary -.->|"Streaming repl"| PGReplica
    API1 --> PGReplica
    API1 --> OPTW
    API2 --> OPTW
    API1 --> Storage
    API1 --> OAuth
    API1 --> TrafficAPI
    Browser --> OSMTiles

    style PGPrimary fill:#336791,stroke:#fff,color:#fff
    style RedisCl fill:#DC382D,stroke:#fff,color:#fff
```

**SLOs:** Disponibilidad 99.5% 5AM-10PM Lun-Sab, RTO 30s failover LB+PG, RPO 5s WAL, Autoscaling CPU 70%.

---

## 6. Trazabilidad Transversal

| RF | Contenedor | Componente L3 | Entidad DB |
| -- | ---------- | ------------- | ---------- |
| RF-001 Flota | API, PG | Services, Repos, Validation | vehiculos |
| RF-002 Pedidos | API, PG, Redis | Validation, Services, Repos | pedidos, clientes |
| RF-003 Rutas | Optimizador, API, PG | OptimizerAdapter, Services | rutas, ruta_pedidos |
| RF-004 Mapa | SPA, OSM | SPA Leaflet | pedidos.gps, rutas |
| RF-005 Dashboard | SPA, API, PG | Services, Cache | rutas.co2/distancia |
| RF-006 Reportes | API, Storage | ReportGen | rutas, vehiculos |
| RF-007 Re-opt | EventBus, Optimizador | EventBus, OptimizerAdapter | rutas (estado) |
| RF-008 Conductores | API, PG | Services, Validation | conductores |
| RF-009 Clientes | SPA, API, PG | Services | clientes |
| RF-010 Compensacion | API, ReportGen | Services, ReportGen | rutas.co2 |

---

## 7. Decisiones Arquitectónicas Clave (ADR)

| ID | Decision | Alternativa descartada | Justificacion |
| -- | -------- | ---------------------- | ------------- |
| ADR-01 | Backend unico Node/Express + TS (sin FastAPI) | MEAN+Mongo, Java Spring Boot | Lenguaje unificado JS/TS, async I/O, open source, costo infra S/500k, memoria JVM alta |
| ADR-02 | PostgreSQL+PostGIS 3FN | MongoDB | Integridad referencial, geo ST_Distance, escalamiento horizontal, TCO |
| ADR-03 | Optimizador aislado Worker Threads | In-process monolitico | SLA 45s/30s sin bloquear event loop, experimentacion metaheuristicas |
| ADR-04 | Redis Cache + BullMQ Pub/Sub | Sin cache / polling DB | Sesiones JWT, rate-limit, cola re-opt debounce |
| ADR-05 | Leaflet+OSM PWA offline-first | Google Maps pago | Codigo abierto, costo, cache teselas, WCAG AA |

---

## 8. Convenciones y Leyenda

* **Notación:** C4 con Mermaid `C4Context/C4Container/C4Component/classDiagram/sequenceDiagram`. Flechas `--> "protocolo"` = HTTPS/TLS1.3/REST JSON/TCP.
* **Colores:** Personas azul, Sistemas azul oscuro, Externos gris, DB rojo, Contenedor interno.
* **Versionado:** V_1_0_0 creación, V_1_1_0 mejora diagramas (este doc). Historial Git preserva V_1_0_0.

---

## 9. Apéndice A: Fallback graph TD (compatibilidad)

Si tu visor no renderiza `C4Context`, usar:

```mermaid
graph TD
    Admin["Administrador<br/>USR-01 CRUD"] -->|"HTTPS/TLS1.3 REST/JSON"| PathSeek["Sistema PathSeek<br/>Angular+Express+PG+Redis"]
    Operador["Operador Logistico<br/>USR-02"] -->|"HTTPS"| PathSeek
    Conductor["Conductor<br/>USR-03 offline"] -->|"HTTPS PWA"| PathSeek
    Cliente["Cliente<br/>USR-04"] -->|"HTTPS"| PathSeek
    Auditor["Auditor<br/>USR-05 READ"] -->|"HTTPS"| PathSeek
    PathSeek -->|"OAuth2 JWT RBAC"| OAuth["OAuth2 IdP"]
    PathSeek -->|"HTTPS JSON<br/>Fallback manual"| Traffic["Waze/Google Traffic"]
    PathSeek -->|"HTTPS Leaflet"| OSM["OSM Tiles"]
    PathSeek -->|"PDF ISO14083"| Report["Storage PDF"]
```

---

## 10. Control de versiones

| Versión | Fecha | Descripción | Responsable |
| ------- | ---------- | ----------------------------------------- | ------------------- |
| V_1_0_0 | 2026-08-25 | Creación inicial del modelo C4 | Equipo del proyecto |
| V_1_1_0 | 2026-09-01 | Mejora diagramas: C4Context/Container/Component, secuencia, despliegue, ADR, fallback | Equipo del proyecto |

---

## 11. Referencia

**Fuente principal:** Consigna PathSeek – Optimizador Rutas Sostenibles UGEL Huancayo. **Docs trazados:** 06 RF-001..010, 07 RNF-001..009, 08 USR-01..05 RBAC, 09 RN-001..012, 10 Stack, 11 DB DDL 3FN, 13 RES-01..21. **Marcos:** C4 Model (Brown), arc42, ISO/IEC 25010, OWASP Top10, WCAG 2.1 AA, ISO 14083, Ley 29733, Ley 30224, DS 033-2012-MTC.
